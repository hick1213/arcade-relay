# S-16 — 难度曲线全阶段与全客人类型（CR-CODE）

## CR-CODE iteration 1 — REJECT
- 日期时间: 2026-09-04T00:55:36Z
- 问题摘要:
  1. [blocker] 老饕「高级菜（4〜6 号菜）」未实现 — `createCustomer`（customerFlow.ts）对全类型统一 `rollDishId(kinds)`，`CUSTOMER.GOURMET_DISH_ID_MIN` 在 src/ 内零引用。tests/difficultyCurve.test.ts 的 `dishId >= 4` 断言仅因 mock `Math.random()=0.5` 恰好算出 4 而空振通过。
  2. [minor] `takeReadyDish`（kitchen.ts）的 JSDoc「未指定時は该客の全菜を引き取る」与实现（findIndex+按 index 过滤，只移除 1 枚）矛盾，语义从「全量」静默变为「首枚」。
  3. [minor] 幻影上菜被静默计为已服务（customerFlow.ts serving 完成时 `servedDish===undefined` 也 `afterDishServed` 递增 dishesServed 并进入 eating）— 被 `?.` 掩盖的静默失败路径。
  4. [minor] `rollCustomerType` 两次独立抽选使老饕実効確率 = (1−w_镖师)×w_老饕（D7–9=4.5% / D10–15=8.5% / D16–20=12%），偏离 gdd L225 的名目权重（≈10%）。
  5. [minor] 离店客人的 ready 菜（及製菜中 ticket）成为孤儿，kitchen 中永不清理；镖师 2 菜流使其更易发生。
- 处理（全部已处理）:
  1. **已处理** — 新增 `rollDishIdFrom(min, kinds)`，老饕菜号改为在 `[CUSTOMER.GOURMET_DISH_ID_MIN, kinds]` 内抽选（`rollCustomerType` 保证 `kinds >= GOURMET_MIN_DISH_KINDS` = min，区间非空）。测试 mock 改为能区分范围的值: `mockReturnValueOnce(0.2).mockReturnValue(0.1)` → dishId = 4（下限そのもの。旧実装なら 1 になりテスト失敗）。注释明记该 mock 値の検出意図。
  2. **已处理** — JSDoc 修正为与实现一致: 「該当客の菜を 1 枚だけ引き取る。未指定時は先頭の 1 枚（全量ではない）。呼出側は常に dishId を指定」。实现侧语义（1 枚のみ）は S-16 の多菜客人仕様に合致しているため実装は不変。
  3. **已处理** — serving 完了時に `servedDish === undefined` なら `dishesServed` を進めず eating にも遷移させず、跑堂のみ解放して return（明示ガード＋コメント。正常経路では dispatchToServe の前提で発生しない異常系）。`?.` の使用も排除。
  4. **已处理** — gdd L225 の名目权重を正として单一乱数を权重区間に分割する抽選に変更（镖师 [0, w_镖师) / 老饕 [w_镖师, w_镖师＋w_老饕)）。依存する 2 件のテスト mock（0.9→0.01 の 2 段 → 単一 0.12 / 0.2）を追従更新。
  5. **已处理** — `tickPatience` で客が離店した場合、残存客の id 集合で `kitchen.tickets` / `kitchen.ready` を filter して離店客の残置を掃除（ticket も掃除しないと後から ready に孤児菜が再生成されるため）。回帰テスト 1 件追加（「離店客の残置菜・製菜中チケットは後厨から掃除される」）。
- 検証: `cd game && npm run typecheck` exit 0（エラー 0 件）/ `npx vitest run tests/difficultyCurve.test.ts` PASS 14 / FAIL 0。lane 規律により `npm run build` は batch 検証区間へ委譲。
- 補足: findings は 5 件すべて解決済み（未处理 0 件）。story status を done に更新。
