# S-17 — 事件卡全 15 张与志向偏移（审查历史）

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-04T01:17:16Z
- 问题摘要（按 finding 顺序。全部 severity: minor）:
  1. `game/src/systems/eventCardData.ts` — 卡 12 选项 1「买十进补丸（银 −18）」为严格劣势选项且结果文案「伙计服后精神百倍，手脚更加麻利」承诺了不存在的正向伙计收益（S-18 仅实现疲劳惩罚、无正向体力 buff），文案与机制永久脱节
  2. `game/tests/eventCard.test.ts` — gdd 效果幅度模板边界值（银 −25/+15、声望 ±10、侠 0–5）在测试中硬编码字面量断言，未集中到 config（tech-stack 规范 1）
  3. `game/src/systems/i18n/zhTable.ts` — 卡 10 标题与 gdd 一览表记为「字序不一致」（声称文案笔误级偏差）
  4. `game/src/systems/eventCardData.ts` — 卡 15 的 gdd 主要影响轴「伙计实力」未建模（仅结果文案表现）— reviewer 自身确认为已记录的合理裁定、非缺陷
- 处理:
  1. **已处理（文案改为纯风味路线）** — `zhTable.ts` / `enTable.ts` 的 `CARD_12_OPT1_RESULT` 改为不暗示任何数值收益的风味文（zh: 「药丸苦涩，伙计们强忍着咽下，仍谢过掌柜的一片心意」/ en: 'The pills are bitter; the staff swallow them and thank you for the thought'）。未采用「加小幅声望补偿」的理由: gdd 一览表卡 12 的主要影响轴为「银/伙计状态」，追加声望 Δ 会偏离轴注记；追加侠 Δ 则会破坏 gdd「侠线可行性算式」的前提（含侠选项卡 = #1/2/3/6/7/11/13/15 的 8 张 — 有对应验收测试锁定）。银 −18 换纯风味结果（体恤伙计的支出）在文案诚实化后属于合法的取舍选项、不再是欺骗性选项。机械数值本身未变（#1/#5/#8 等既有数值不动的原则同样适用）
  2. **已处理** — `config.ts` 的 `EVENT` 追加 `SILVER_DELTA_MIN(-25)/SILVER_DELTA_MAX(15)/REPUTATION_DELTA_MIN(-10)/REPUTATION_DELTA_MAX(10)/XIA_POINT_MAX(5)`（仅追加、不改既有行），`eventCard.test.ts` 的幅度边界断言全部改为引用本定数。`eventCardData.ts` 头部注释同步追注定数出处。卡数据表内每卡具体数值按 finding 指示维持内容数据直值
  3. **暂不处理（finding 不成立 — 反证）** — 逐字节比对确认 `design/gdd.md` L190 与 `zhTable.ts` L170 的卡 10 标题为**完全相同的字符串**「婚礼包席」（U+5A5A U+793C U+5305 U+5E2D — 两侧一致，无字序差异）。en 侧 'A Wedding Banquet' 亦无问题。未做任何修改（无差异可对齐）
  4. **暂不处理（已记录的合理裁定）** — 与 finding 指摘一致，`state/stories.yaml` S-17 note 已记录理由（属性增减机制不存在、不越权自创系统、gdd 轴注记为非约束性备注）。reviewer 亦确认为非缺陷、仅为审查留痕。无追加变更
- 验证: `cd game && npm run typecheck` → exit 0（lane 内降级验证 — `npm run build` 按并行 lane 规律留待 lane 合流后的批量验证区间）。`npx vitest run tests/eventCard.test.ts` → 13 passed / 0 failed（本 story 测试范围内确认）
- 判定经过: 到达 CR-CODE MAX_ITER（1）→ 上记 4 件中 2 件修正、2 件以反证/既有记录暂不处理 → S-17 status 更新为 `done`，未处理的 finding 3/4 的要旨记入 stories.yaml note（Checkpoint 向人类展示）
