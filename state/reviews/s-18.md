# S-18 — 三属性日间效果与疲劳（审查历史）

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-04T01:40:44Z
- 问题摘要: (1) [minor] chooseOption の mayFatigue 接线（eventCard.ts:74-82）が无测试覆盖 — Math.random() の roll 与「全伙计均匀随机选 1 名」的目标选择が内联で不可注入、回归（条件反转/targetIndex 计算错误/分支删除）时既存测试は全て通過してしまう (2) [minor] 疲劳状态に玩家可见の表现面が无い — member.fatigue の唯一消费者は customerFlow.actionDurationMs（耗时 ×1.2）、UI/i18n/HUD は未読、 fatigue UI 标记の story も无し。P-02「修练后日间可见差异」の载体定位との落差。S-18 acceptance 自体は UI を含まないため tech-director の范围判定を求む (3) [minor] customerFlow.ts:194 の return 行约 115 字符が既存换行宽度（约 90 列）を超え、prettier hook で再整形され后续 diff を污染する恐れ
- 处理:
  - finding 1（chooseOption mayFatigue 接线无测试）: **已处理** — `game/tests/eventCard.test.ts` に `vi.spyOn(Math, 'random')` による确定性测试 4 件を追加（roll 成立 → staff[0] に疲劳＋nightFatigueIds 记录 / roll 不成立 → 疲劳无し / 対象選択の均一 random（2 回目の値 0.5 → floor(0.5×5)=2 → wenqu）/ mayFatigue 以外の选项では Math.random 不呼出）。rng を chooseOption に注入する方式は既存呼出侧（scenes）の API 变更を伴うため、spying 方式（fix 提案の第一选项）を採用。実装中に判明した vitest 4 の挙動: describe スコープで spy を 1 回生成し afterEach で mockRestore() すると、2 つ目以降のテストで spy が無効化され（mock.calls: 0、once 指定が無視される）元実装が走る — 対策として spy は各テスト内で生成し afterEach は vi.restoreAllMocks() に統一
  - finding 2（疲劳の玩家可见表现面なし）: **暂不处理（范围裁定を上报）** — S-18 acceptance は「三属性公式 + 疲劳次日生效 + 单测」であり疲劳 UI 标记を含まない。UI/i18n 层は ui-engineer の担当范围で、gameplay-engineer が story に无い UI を自创するのは「禁止跳过 tier」に违反。現状の表现面は (a) 疲劳次日の动作耗时 ×（体力减免后倍率）= 动作速度の肉眼可感な差分（P-02 の载体そのもの）(b) 事件卡の resultText（疲劳を口头提及）。tech-director 判定事项として「疲劳 UI 标记の追加 story 要否」を Checkpoint 経由で上报（state/stories.yaml の S-18 注记に记録济み）
  - finding 3（customerFlow.ts:194 长行）: **已处理** — actionDurationMs の return を多行の括弧付き乗积表达式に折り返し（约 90 列内。ロジック不変）
- 验证: `cd game && npx vitest run tests/eventCard.test.ts tests/dayEffects.test.ts` → 32 PASS / 0 FAIL、`npm run typecheck`（tsc --noEmit）→ exit 0。lane 指示により `npm run build` は并行 lane 中は不执行（合流后の批量验证区间で実施）
