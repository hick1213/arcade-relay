# S-19 — 终战系统与重试当日（CR-CODE）

## CR-CODE iteration 1 — REJECT
- 日期时间: 2026-09-04T02:16:49Z（`date -u` 实测）
- 问题摘要（按优先级）:
  1. [blocker] `GameScene.goToResult` 无条件附带 `preBattleSnapshot`，违反 types.ts 契约「finalBattleLoss 时のみ付与」。战胜/手動「结束周目」后的「再来一周目」实际回到终战前 + applyRunResult 二重適用（finished_runs/served_total 双重计数）
  2. [major] `GameplayView.buildStructuralKey` 不含 `finalBattle.status/aidHired` — hireAid 后战力/援助按钮 UI 不更新
  3. [major] finalBattleLoss 的周目终结 persist 仅接线在 Result「回到菜单」。开战前暂停→「回到菜单」或 Result 放置（标签关闭/刷新）时 SaveData.run 残留陈旧快照，Menu「继续周目」复活死周目
  4. [minor] `buildBattlePanel` 魔法数字 `NIGHT.PANEL_X ± 180`（战力标签/数值列偏移）
- 处理:
  1. 已处理 — `goToResult` に `summary.kind === 'finalBattleLoss'` でのみ `run.finalBattle?.preSnapshot ?? null` を付与へ修正（game/src/scenes/GameScene.ts）。他 kind は契約どおり null。「再来一周目」は payload なし＝新周目、二重適用解消
  2. 已处理 — `buildStructuralKey` に `run.finalBattle ? \`${status}:${aidHired}\` : '-'` を追加（game/src/ui/GameplayView.ts）。hireAid で status/aidHired 以外不変のため鍵組込みが必須（戦力表示は playerPowerOf(run) が aidHired 依存）。rebuild が再走し战力 +8 と按钮不活性化が反映される
  3. 已处理 — 2 段で対処（提案の両候補を採用）: (a) 終戦敗 Result 入場時点で run 快照を破棄（新規 `discardRunSnapshot`: `run := null` のみ、meta 適用はしない — 重试当日なら失敗として計上しない契約を保持。これで Result 放置/刷新でも「继续周目」が死周目を复活させない）; (b) 開戦前（`finalBattle !== null && ended === null`）で Pause「回到菜单」する時は現行 run を `persistRunSnapshot` してから遷移 — 第 1 日晨への巻き戻りを解消し、実質ある live run として再開可能（敗北していなかったため失敗計上はしない。提案候補 1 の「abandonFinalBattleLoss を適用」は未発生の敗北を計上するため不採用、理由明記）
  4. 已处理 — `config.ts` の `BATTLE_UI` に `POWER_LABEL_OFFSET_X: -180` / `POWER_VALUE_OFFSET_X: 180` を追加し `buildBattlePanel` で使用（config.ts への追加のみ、既存行の変更なし — 並行 lane の競合回避）
- 验证: `cd game && npm run typecheck` exit 0（编辑文件起因的错误 0。並行 lane 中のため `npm run build` は不執行 — tech-stack.md「検証コマンド」节、lane 合流後の批处理検証が最終確認）
- 判断事項: finding 3 の (b) は Menu 復帰先が「第 20 日晨間（S-04 快照契約 — 全状態復元は S-23 拡張）」になる。终战開戦前の完全復元は S-23（run 快照拡張）以降の改善対象
- ステータス: MAX_ITER 到達につき findings 全件処置済みで S-19 を done へ更新（stories.yaml 注記に処置記録）
