## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-04T02:39:26Z
- 问题摘要:
  1. (major) 手動経路（GameScene PausePanel「结束周目」→ buildRunEndSummary(run, 'runComplete')）が
     無条件で结局判定を発火 — 终战を战わずに三线 0 の argmax（wealth 险成）＋endingBonus 200 の
     persist＋endings_seen 置位が可能。gdd「胜负条件」の前提（终战胜利 → 结局判定）を call site が守っていない
  2. (minor) judgeEnding の防御フォールバックが志向線を返す（決勝規則 = gdd 記載順先頭と不一致）。
     また reduce 初期値 0 のため top が真の最大値でなく 0 にクランプされ得る（現状到達不能）
- 处理:
  1. 已处理 — ゲートを systems/economy.buildRunEndSummary 内に実装（纯函数・Scene に論理を置かない）:
     `run.finalBattle?.status !== 'won'` の runComplete 要求は结局判定を不発とし
     ending なし・endingBonus 0・closeCall なしの败局同型 summary を返す
     （GameScene 側 call site は変更なし。正当経路 = finalBattle.fight の resolved は status 'won'
     を渡すため影響なし）。tests/ending.test.ts に回帰テスト 2 件追加
     （未開戦/lost の runComplete 要求で结局捏造しない／endings_seen 不置位）。
     既存 runComplete 系テストは makeRun に battleWon オプションを追加して対応
  2. 已处理 — reduce 初期値を先頭要素の値に変更（top は常に真の最大値）＋
     到達不能分支のフォールバックも決勝規則に一致させ ACHIEVEMENT_ORDER 先頭の tied[0] ?? head を返す
     よう修正（ending.ts）
- 验证: `cd game && npm run typecheck` exit 0 ／ `npx vitest run` 121 件 PASS 0 FAIL
  （並行 lane につき `npm run build` は lane 合流後の批处理验证区间で実施 — tech-stack.md「验证命令」）
- 判定: findings 全件处理済み。stories.yaml S-20 → done（MAX_ITER 到達に伴う処理 — 未处理 finding なし）
