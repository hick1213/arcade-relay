# S-21 — 成就判定（ACH-01～06）审查历史

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-04T03:00:58Z
- 问题摘要:
  1. (minor) `config.ts` `META_SAVE.ACHIEVEMENT_IDS` 与 `metaTypes.ts` 的 `AchievementId` 联合类型为两个并行真理源、无编译时链接 — `metaSchema.ts` / `achievements.ts` 的 `{} as Record<AchievementId, boolean>` 转换会静默接受偏差列表（config 侧 typo → 默认存档缺少真实 ACH-xx 键、运行时 merge/diff 对 undefined 操作）
  2. (minor) `GameScene.persistRunResult`（~212 行）与 `ResultScene.abandonFinalBattleLoss`（~95 行）的 `saveSaveData(updated)` 未防护 QuotaExceededError — persistRunResult 中未捕获异常会在 `scene.start('Result')` 前中止、玩家卡在 GameScene（既有模式的延续、非新引入）
  3. (minor) 破产路径上成就提示音（SFX-05 achievement 变调）与 ResultScene.create 的 sfxFailLeave 同刻重叠；终战败→回到菜单时也在场景切换瞬间触发 — 润色建议（acceptance 仍满足）
- 处理（gameplay-engineer）:
  1. **已处理** — `metaTypes.ts` 的 `AchievementId` 改为 `(typeof META_SAVE.ACHIEVEMENT_IDS)[number]` 编译时派生（`import type` 仅类型位置、运行时零依赖）。比 reviewer 提议的 `as const satisfies` 更强: 派生使「config 数组 = 类型」双向绑定，config 漏记/误记 id 即编译错误，且 `judgeAchievements` 的显式对象字面量若未覆盖新 id 也编译失败 — 两源偏差在构造上不可能。验证: `npm run typecheck` exit 0 + `npx vitest run tests/achievements.test.ts tests/metaSave.test.ts` 22 件 PASS
  2. **已处理** — 两处 `saveSaveData` 均以 try/catch 防护，catch 时 `console.error('[SaveWriteFailed] ...')` 显式记录（error 级 — 不降级为 warn，silent-failure 规范遵守）并允许导航继续（Result / Menu 迁移不再被写入失败中断）
  3. **已处理** — 失败终结路径仅延迟再生: `config.AUDIO.ACHIEVEMENT_FEEDBACK_DELAY_MS`（新常量、700ms）追加。GameScene.persistRunResult 仅 `kind === 'bankruptcy'` 时经 `setTimeout` 在 sfxFailLeave 之后再生（runComplete 胜利路径无重叠、维持即时再生）；ResultScene.abandonFinalBattleLoss（终战败→Menu）同样延迟。提示音本体不变（独特音需新资产 — 超出本 story 资产范围）
- 验证: `cd game && npm run typecheck` exit 0（lane 内降级验证 — `npm run build` 留给 lane 合流后的批量验证区间）/ vitest 成就·存档相关 22 件 PASS
