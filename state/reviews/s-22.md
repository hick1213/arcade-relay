# S-22 — 解锁（UNL-01/02）与初始伙计选择

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-04T03:27:13Z
- 问题摘要（4件、全て minor）:
  1. `src/systems/staffSelect.ts` — selectCandidate が「既に编成内の候補」を選択可能にし、以後の置換 tap が replaceStaffMember の some() ガードで黙って無視される死路 pending になる
  2. `src/systems/runEngine.ts` — createRunSnapshot（S-04）が day/silver/reputation/ambition のみ保存し、「继续周目」で置換した解锁伙計が静默に默认编成へ戻る（S-23 の快照拡張までの窗口）
  3. `tests/staffSelect.test.ts` — 「解锁伙计で默认伙计 1 名を置き換える」テストが UNLOCK_STAFF 自身を期望值にする恒真断言で、config の gdd 转写ミスを検出できない
  4. `src/systems/runEngine.ts` — createDefaultUnlocks がリテラル `{ 'UNL-01': false, 'UNL-02': false }` をハードコードし metaSchema の reduce と重複（新解锁 id 追加時に陳腐化リスク）
- 处理:
  1. **已处理** — selectCandidate に「编成内に同 id が既に在れば不発」のガードを追加（死路 pending を発生させない）。再 tap で解除→再選択の挙動は不变。対応テスト「既に编成内の候補は選択できない」を追加（再 tap で pending が null のまま・置換 tap も不発を検証）
  2. **暂不处理（本 story の範囲外 — 注记で上报）** — 修正は RunSnapshot 契約の拡張（伙计表の快照への追加・復帰時の適用）であり、S-23「中断续玩 run 快照（日粒度）」の acceptance が既に「伙计表」を含むため S-23 の委派先。本 story では state/stories.yaml の S-22 status 注记に「S-23 で伙计表を快照に含めること（置換した解锁伙計がそれまで静默に失われる）」を記録し、lane 合流後の Checkpoint で人間に表示される
  3. **已处理** — 期望值を gdd「解锁」表 UNL-01 のリテラル（速 2/艺 1/体 4、id 'liubiaotou'）に変更。config 转写ミスは本テストで検出される（現状は gdd 275-276 行と一致を確認済み）
  4. **已处理** — createDefaultUnlocks を `META_SAVE.UNLOCK_IDS.reduce(...)` による派生に変更（metaSchema.createDefaultSaveData と同形。新解锁 id 追加時に自動追随）
- 验证: `npm run typecheck` exit 0（自己编辑文件の错误 0）/ `npx vitest run tests/staffSelect.test.ts` 17 件 PASS 0 FAIL（追加 1 件を含む）。lane 中のため `npm run build` は批处理验证区间で実施
- 备注: MAX_ITER（1）到達につき S-22 を status: done。未処理 finding 2 件目は上記のとおり注记に記録済み（Checkpoint 展示対象）
