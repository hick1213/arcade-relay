# S-25 — Result 结局演出完整版（审查历史）

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-04T00:43:02Z
- 问题摘要（按优先级）:
  1. [major] ResultPanel.ts — 败局文/结局文（BODY_OFFSET_Y=-122、origin 0.5,0 向下展开）与 SCORE_LABEL（-96、20px）布局冲突。单行时已贴近，BODY_WRAP_WIDTH=520 内 2 行换行（en 破产文 72 字符必然命中）时第二行与标签文字区直接叠字
  2. [minor] enTable.ts / zhTable.ts — ZH 结局题带「财结局 — 」等类型前缀、EN 无 → 跨语言信息量不对齐
  3. [minor] ResultScene.ts — playResultSfx 的 locked 分支: unlock() 非同步，紧随的 sound.play 会被静默丢弃（仅内部 warn 1 次、无显式记录）
  4. [minor] ResultPanel.ts — 明细行 Math.round 与总评分 floor 的口径差。WEIGHT_SILVER=0.5 × 奇数银子时显示行之和与总分差 1～2
- 处理（ui-engineer，全件对应 — 未处理 0 件）:
  1. **已处理** — body 改为**下端锚定**（config.ts `RESULT.BODY_OFFSET_Y`: -122 → **-110**、ResultPanel.ts `setOrigin(0.5, 0)` → `(0.5, 1)`）。多行换行时向**上**伸長，与总评分ラベル（SCORE_LABEL_OFFSET_Y=-96、不变 — S-15 布局键最小改动）的间隔恒定。标题侧 `RESULT.TITLE_OFFSET_Y`: -158 → **-176** 退避，确保 2 行换行（36px）まで title/body 非重叠（panel 顶 -230 まで余白 34px）。3 行以上换行时の title 側微重叠は残存するが、现行 2 言语表の最长文（en 破产文）≈ 2 行で収まるため実害なし — 短文は zhTable/enTable の文案規約（1〜2 行）で担保
  2. **已处理** — review 推奨の後者（より簡潔）を採用: zhTable.ts の结局题から「财结局 — 」「侠结局 - 」「名结局 - 」类型前缀を除去（财通四海 / 侠名远扬 / 名动天下）。EN 表は変更なし。结局类型の同定は结局插画＋结局文で担保
  3. **已处理** — locked 分支を即 play から「`sound.once(Phaser.Sound.Events.UNLOCKED, ...)` 発効後に再生」へ変更（unlock() 呼び出しは維持）。音が静かに捨てられる経路を解消し、volume は分岐前に一度だけ SaveData から取得
  4. **已处理** — formatBreakdown の贡献表示を `Math.round` → `Math.floor` に統一（raw は整数カウントのため表示不変）。`floor(silver×0.5 + 整数其余)` = floor 済み各行の和 が厳密に成立し、显示行合計=总评分の食い違いを解消
- 验证: `cd game && npm run typecheck` → **exit 0**（lane 規律により `npm run build` は不执行 — 批处理验证区间で実施）
- 状态: 到达 MAX_ITER（review-loops.md CR-CODE MAX_ITER=1、findings 解决済み）→ state/stories.yaml S-25 → **done**
