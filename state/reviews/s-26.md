# S-26 — Menu 游戏外面板完整版（图鉴/成就/统计）

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-04T00:57:16Z
- 问题摘要:
  1. [major] config.ts:477,492 — `SLOT_LABEL_FONT_SIZE` / `ACH04_VALUE_FONT_SIZE` 未接线（结局格 label 与 ACH-04 进度值均走 `ROW_FONT_SIZE`，调参无效 + en 长结局名相邻格重叠）
  2. [minor] MenuStatsPanel.ts:172 — 进度值 X 位置的字面量 `14` 违反 config 集中规范
  3. [minor] textKeys.ts:152 — 死键 `MENU_PANEL_SLOT_LOCKED`；未解锁图鉴格直接显示真实结局名（剧透）
  4. [minor] MenuStatsPanel.ts:82,86-88 — `columnX[index] ?? centerX` 防御性回退会掩盖配置错误；附带 config.ts:462 注释「配色仅 UI.PANEL_* 系」与新直书值不符
- 处理:
  1. **已处理** — 接线专用样式: 结局格 label 用 `MENU.SLOT_LABEL_FONT_SIZE`、ACH-04 进度值用 `MENU.ACH04_VALUE_FONT_SIZE`（`rowStyle` 增加 fontSize 参数，默认值维持 `ROW_FONT_SIZE`）。附带: 结局格 label 追加 `setWordWrapWidth(MENU.SLOT_GAP)`（折返幅=格中心间隔），从样式层面消除 en 长结局名的相邻格重叠
  2. **已处理** — 提取为 `MENU.ACH04_VALUE_X_GAP: 14`（config.ts MENU 块内追加，本 story 所需常量）
  3. **已处理** — 采用原设计意图: 未解锁格渲染 `MENU_PANEL_SLOT_LOCKED`（'Locked'/'未解锁'），仅解锁格显示真实结局名（防剧透）。死键消除
  4. **已处理** — 删除 `?? centerX` 回退，改为构造函数内显式长度检查（`SECTION_COLUMN_X_OFFSETS.length !== SECTION_COUNT(3)` 时 throw Error 并附 actual 值 — 配置错误在开发期立即可见，不再静默堆叠到画面中央）。附带: config.ts:461-462 注释修正为「UI.PANEL_* 系の参照に加え、ロック态のみ art-bible 调色板由来の直書き値（SLOT_FILL_LOCKED / LOCKED_TEXT_COLOR）」
- 验证: `cd game && npm run typecheck`（tsc --noEmit）exit 0、错误 0 件。并行 lane 中 — `npm run build` 按 tech-stack.md「验证命令」节留给 lane 合流后的批处理验证
- 遗留未处理事项: なし（全 findings 已处理）。S-26 → status: done
