# S-27 — 音频接线与音量实效（CR-CODE 履历）

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-04T01:33:40Z
- 问题摘要（6 件 — major 2 / minor 4）:
  1. [major] audioTriggers.ts — 破产 SFX 双重播放: ended 差分 cue 与 ResultScene.create 的同一 cue 在同一次败局上必然连发
  2. [major] audioTriggers.ts — hasFailedDeparture 在日→夜「残客清扫」（advanceDayPhase 清算なし退場）时把非服务失败误报为耐心归零 → SFX-06 在每个日夜交替帧错误发声
  3. [minor] audioTriggers.ts — FIGHT_CONFIRM tap cue 与「进入第 20 日夜」相位差分 cue 的双重 gong 隐患（S-19 接线后成立）
  4. [minor] AudioDirector.ts — applyBgmVolume 直接写 volumeNode.gain 绕过 volume 属性，可观测状态（currentConfig.volume）与实际增益不同步
  5. [minor] config.ts — EVENT.SILVER_DELTA_* / REPUTATION_DELTA_* / XIA_POINT_MAX 无代码引用，注释「テストは本定数を参照」与实际不符
  6. [minor] audioTriggers.ts / AudioDirector.ts — 每帧 Set/Map 分配（hasFailedDeparture/hasServeCompleted）；playSfx 在 manager === null 时静默 return
- 处理（全件处置完毕、未处理 0 件）:
  1. **已处理** — audioTriggers.ts から ended 差分の bankruptcy cue を削除。敗局演出音（SFX-06 低速变调）の所有者は ResultScene.create に一本化（ResultScene 側は変更なし）。削除箇所に理由コメントを記載
  2. **已处理** — hasFailedDeparture の判定信号を客集合差分から `next.daySummary.failed > prev.daySummary.failed` の増分に変更。daySummary.failed は tickPatience の真实归零时のみ加算され（customerFlow.ts:294）、advanceDayPhase の日終了清算では加算されないため誤検知しない。daybreak で 0 にリセットされる迁移は next < prev となるため誤検知なし。isWaitingStage ヘルパーは不要となり削除
  3. **已处理** — 相位差分の gong cue に `tapEvent !== TAP_EVENTS.FIGHT_CONFIRM` guard を追加。S-19 が FIGHT_CONFIRM で直接 night+finalBattleNight へ進める場合も排他的に保たれる（先行排除 — S-19 接线時の確認事項を解消）
  4. **已处理** — applyBgmVolume に `sound.currentConfig.volume = value` の同期を追加（cancelScheduledValues+setValueAtTime の回避自体は正当なため維持）。`sound.volume` の getter は `volumeNode.gain.value` を読むため QA の自動検証（gates.md QA-PLAY 要点 2）は本適用 1 render quantum 後に反映値を読める。currentConfig は d.ts 漂れ（BaseSoundManager 側にのみ宣言、実体は BaseSound が保持）のため unknown cast でアクセス
  5. **已处理（注释修正で対応）** — 定数自体は維持（systems/eventCardData.ts の效果幅度コメントが参照先として本定数を指しており、並行 lane の検証テストでの参照可能性があるため削除はリスク）。コメントを実際と一致するよう修正（「コードからの参照はまだ無く、検証テスト lane での参照を想定した先行定義」）
  6. **部分处理** — hasFailedDeparture の毎帧 Set 生成は 2 の修正（daySummary 増分）で消滅。hasServeCompleted は毎帧 Map 生成が残るが、SFX-04 の発音タイミングの正しさ（dishesServed 増加/eating 迁移の瞬間）を daySummary.served（collectCustomer＝収钱時に加算）で代替すると発音が遅れるため、客数個別の diff 判定を維持（客数は個別、実害なし — 判断理由を明記）。playSfx の manager === null 静默 return は 1 回限りの `console.warn('[AudioDirector] playSfx called before attach() — SFX dropped')` を追加（現行の呼び出し系は全て attach 後で到達不能な防御分岐）
- 验证: `cd game && npm run typecheck` → exit 0（lane 中のため `npm run build` は并行 lane 規律により批量验证区间で実施）
- verdict 反映: findings 全件に处置記載（未处理 0 件）→ 合格基準「findings 已解决」を満たす。MAX_ITER 到達に伴い stories.yaml の S-27 を done へ更新
