# S-28 打磨: 差分强化与演出细节 — CR-CODE 审查历史

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-04T01:59:16Z（verdict 受领。reviewer 由 workflow 转达 — findings JSON 7 件、全件 minor）
- 问题摘要:
  1. stagePresentation: 黑烟/金光が掌勺の阶段のみに紐づき、残影/ぐらつき（staff 条件付き）と扱いが不整合 — gdd 差分表は黑烟/金光を铁牛本人の差分と規定
  2. stagePresentation: headChefStaff/headChefStage が systems/kitchen.headChefCraft の選定ルールを ui 側で再実装（二重管理 — kitchen 側変更時に静的に乖離）
  3. GameplayView: 表情贴片の z-order/几何が名前ラベル（centerY−26）と衝突の疑い（frown 口が名前行に乗る）
  4. GameplayView: 黑烟/残影の fade tween が rebuildTweens 未登録 — rebuild/destroy 中に tween 残置＋onComplete 二重 destroy の恐れ
  5. stagePresentation: 纯函数モジュールなのに unit test が無い（既存 vitest suite の慣行に反する）
  6. GameplayView: 914 行で 800 行ガイドライン超過（motion/fx methods を ui helper に抽出可能）
  7. stories.yaml（プロセス注記）: レビュー対象指定 commit が status 変更のみで実装が 3 commit に分散していた

## 处理（ui-engineer、2026-09-04T02:09:09Z 実施）
- 日期时间: 2026-09-04T02:09:09Z
- finding 1 — **已处理**: stagePresentation.ts に SMOKE_STAFF_ID/GLOW_STAFF_ID（'tieniu'）＋SMOKE_STAGE(0)/GLOW_STAGE(2) のゲートを追加し、`headChefFx(run): {smoke, glow}` を新設。GameplayView は `dayHeadChefStage` を `dayHeadChefFx` に置き換え、铁牛本人が掌勺のときのみ黑烟/金光が出る（gdd 差分表どおり）。
- finding 2 — **已处理（部分・getter 依頼は継続）**: ui 側の選定ルール再実装を廃止し、`systems/kitchen.headChefCraft(run)` の値を直接参照して「craft === 最高值の最初の非修练メンバー」を引く方式に変更（systems 側の短縮ルール変更には自動追従）。identity getter は systems 側に未実装のため同値照合が残る — getter 追加依頼は gameplay-engineer への判断事項として報告済み（gameplay-engineer 職域のため本レーンでは実装しない）。
- finding 3 — **已处理**: config STAGE_FX.EXPRESSION.HEAD_FROM_TOP_RATIO を 0.18→0.10、MOUTH_FROWN_DY を 11→9 に変更し、顔域（口下端 = centerY−0.4×表示高さ＋9）を名前ラベル帯より上へ退避。名前ラベルは新定数 MORNING.AVATAR_NAME_OFFSET_Y（18）に移動（従来の直書き 26 を解消）。最小缩放 1.0 でも約 3px の空き。最終的な見た目は QA-PLAY の目視で確認。
- finding 4 — **已处理**: spawnTrailGhost/emitSmokePuff の fade tween を rebuildTweens に登録（rebuild/destroy で remove — container.destroy(true) が子を破棄するため破棄漏れなし）。
- finding 5 — **已处理**: tests/stagePresentation.test.ts を新設（12 件 — 未登记 id フォールバック、伙计別 tint/bob、残影=阿福高阶のみ/ぐらつき=小蝶低位のみ、headChefStaff の max/同值 tie-break/修练除外/不在 null、headChefFx の铁牛ゲート）。`npx vitest run tests/stagePresentation.test.ts` PASS(12) FAIL(0)。
- finding 6 — **暂不处理**: GameplayView.ts 914 行（ガイドライン超過、ルール違反ではない）。motion/fx helper への抽出は並行レーン稼働中のリファクタ churn を避けるため見送り — 表示写像は既に stagePresentation.ts へ抽出済みで、残部は Scene 描画の厚み。lane 合流後の整理候補として Checkpoint で報告。
- finding 7 — **已处理（プロセス）**: コード変更不要の注記。以後の自レーン commit は 1 story = 1 メッセージで実装本体を含む単一 commit を基本とする。
- 検証: `npx tsc --noEmit` → エラー 0（exit 0）。`npm run build` は並行レーン規律により lane 内では実施せず（lane 合流後の批処理验证で最終確認）。
