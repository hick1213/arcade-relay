# S-32 — UI 模糊排查与可读性修复（审查历史）

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-03T15:40:19Z（`date -u` 实测）
- 问题摘要:
  1. (minor) config.ts — S-32 コミットに S-33 スコープの ASSET_KEYS.images/spriteSheets 登録（~48行）が同梱。キーは不活性（本コミット時点で読込なし）で機能影響なし。reviews 注記 or S-33 への帰属明記を提案
  2. (minor) config.ts — TEXT_RESOLUTION 固定 2 は >2560px 幅ウィンドウ（4K ≈ 3x FIT 拡大）で実効レンダリング解像度を下回る可能性。0.5x/2x acceptance は充足するが、定数コメントに上限の明記 or devicePixelRatio 由来化を提案
  3. (minor) config.ts — ASSET_KEYS.images ブロックのコメント「audio と同一規約」が不正確（audio は拡張子なし・images は .png 付き）
- 処理:
  1. **暫不処理（理由記載）** — キー削除は S-33 の進行中 WIP（未コミット systems/visualAssets.ts、別 lane）と衝突するリスクがあり、並行 lane 規範（共有ファイルの既存行保護）に反する。キーは不活性で機能影響ゼロのため、config.ts の images ブロックコメントに「登録は S-32 コミットに同梱」の帰属注記を追記し、本ファイル＋stories.yaml S-32 notes に記録 → Checkpoint で人間に提示
  2. **已处理** — TEXT_RESOLUTION のコメントに 4K（≈3x FIT）での 2x 上限と window.devicePixelRatio 由来化の選択肢を明記
  3. **已处理** — images ブロックのコメントを修正（.png 拡張子を含む点・audio は OGG/M4A 双形式展開のため拡張子なし＝規約は同一ではない旨）
- 検証: `cd game && npm run typecheck` — 自己編集ファイル（config.ts）起因のエラー 0。残存 30 件はすべて src/ui/GameplayView.ts（並行 lane の WIP — 未提供の GAME_LAYOUT/GAME_HEIGHT 参照）で lane 合流後のバッチ検証で解消見込み。**並行 lane 期間のため `npm run build` は不実施**（tech-stack.md「検証コマンド」節）
- 判定対応: MAX_ITER 到達（CR-CODE iteration 1 回のみ）につき findings 2/3 件修正・1 件注記で stories.yaml S-32 を status: done に更新。暫不処理の 1 件は Checkpoint C の未解決事項として展示される
- コミット帰属の追記（2026-09-03T15:44JST 頃確認）: config.ts / stories.yaml への本修正（finding 2・3 のコメント修正＋S-32 status: done＋notes）は、並行 lane の S-33 コミット `e630ab3cb5e6fa73f66991b975422eba2913dfc5`（共有ファイルの all-or-nothing コミット、本コミット 12 秒前）に同梱された。内容は HEAD で検証済み（コメント 2 箇所・status: done 行とも存在）。S-32 名義のコミット `aa1ec98305cf3a4a43e2a4223cc60779a897145d` には state/reviews/s-32.md が含まれる。帰属混在は finding 1 と同種の並行 lane 制約 — Checkpoint で併せて提示

## CR-CODE iteration 2（委託側からの findings 再送審査）— CONCERNS → 対応済
- 日期时间: 2026-09-04T00:29:07Z（`date -u` 実測）
- 問題摘要（委託側プロンプトの findings JSON。it.1 とは別セット — resume による再送）:
  1. (minor) config.ts / state/stories.yaml — S-32（UI ぼやけ修正）名義のコミットに S-33 スコープの ASSET_KEYS.images/spriteSheets 登録（~35行・コメント自身が「S-33」と記載）と S-30/S-31/S-33 の story 定義（stories.yaml 35行）が同梱。story 単位の CR-CODE 评审单元が崩れ、`git log -- <path>` での原因 story 特定が曖昧になる。提案: story 1 件 = コミット 1 件
  2. (minor) game/src/ui/Hud.ts:81-97 — Hud.flash の Δ ポップアップ数字が数値変動のたびに Text を新規生成（TEXT_RESOLUTION=2 光栅化で 4 倍像素量）し tween 完了後に destroy。頻繁な加算時に GC 圧力。提案: chip 毎に単一 popup Text を再利用（tween 後 setVisible(false)）or オブジェクトプール
  3. (minor) config.ts:180-185 — 効果性注記（コード欠陥ではない）: TEXT_RESOLUTION=2 は 1280x720 画布内 2x 超采样で、Scale.FIT 下の画布レベル CSS 拡大のぼやけは対象外。コメントは 4K（≈3x）不足と devicePixelRatio 由来化を自己言及済み。QA-PLAY で 2560px 実窓の acceptance「无肉眼可见模糊」検証を義務付け
- 処理:
  1. **暫不処理（理由記載 — it.1 finding 1 と同一事象）** — 既にコミット済みの歴史を後から拆分することは並行 lane が稼働中の index を共有する現状ではリスクが大きく、また対象キーは不活性（本タイミングで読込なし）で機能影響ゼロ。帰属注記は config.ts コメントと stories.yaml S-32 notes（it.1 分）に記録済み。以後の自レーン コミットは「自分が編集した単一ファイルパスのみの git add＋`git commit -- <paths>` 指定形式」を徹底し、再発を防止。stories.yaml の story 定義同梱も同様の並行 lane 共有ファイル制約によるもの — Checkpoint で人間に提示
  2. **已处理** — HudChip に `popup`（chip 毎に 1 個を createChip で事前生成・非表示）と `popupTween` ハンドルを追加。flash 時は `runPopup` で既存 Text を再利用（setText/位置リセット/setAlpha(1)/setVisible(true)）し、進行中 tween は `remove()` して差し替え。毎回の Text 新規生成＋destroy を廃止し GC 圧力を解消。オブジェクトプールまでは不要（chip は 3 個固定＝同時 3 popup の上限）
  3. **既に処理済み（it.1 finding 2 で対応）＋ QA 引き継ぎ注記** — config.ts TEXT_RESOLUTION コメントに 4K（≈3x FIT）での 2x 上限と window.devicePixelRatio 由来化の選択肢を明記済み。コード修正は本 finding も「固定 2 で acceptance（0.5x/2x）は充足」を前提に不要と判断。2560px 幅実窓での「无肉眼可见模糊」実証は QA-PLAY Gate の検証項目として引き継ぐ（stories.yaml notes に記録）
- 検証: `cd game && npm run typecheck` — exit 0（自己編集ファイル src/ui/Hud.ts 起因エラー 0。並行 lane WIP 起因の残存エラーも 0 に収斂）。**並行 lane 期間のため `npm run build` は不実施**（tech-stack.md「検証コマンド」節 — dist/ 衝突防止）
- 判定対応: 委託側指示により MAX_ITER 到達扱いで S-32 status: done（stories.yaml 注記に未処理 1 件・QA 引き継ぎ 1 件を記録。いずれも Checkpoint C で展示）
