# S-33 — 真实资产接入与视觉整合

## CR-CODE iteration 1 — CONCERNS
- 日期时间: 2026-09-03T15:52:16Z（`date -u +%Y-%m-%dT%H:%M:%SZ` 実測）
- 問題摘要: major 3 件（IMG-30 未接线・加载失败静默・立绘尺寸低于 assets.md 带）＋ minor 5 件（sheet fallback 结构・防御路径无告警・SPRITE_PLATE_ALPHA 注记矛盾・EVENT_CARD_WIDTH 死常量・柜台/出餐口纯色矩形）
- 处理:

| # | 严重度 | 问题 | 处理 |
|---|---|---|---|
| 1 | major | IMG-30（ui-common-sheet.png）在 BootScene 加载但全代码库无帧索引引用，Hud/MenuScene/各 Panel 仍用 uiPlaceholder 程序化占位纹理 — acceptance「UI 用 IMG-22～25/29/30」未达成 | **暂不处理＋拆分推荐**。置换对象（game/src/ui/Hud.ts、MenuScene.ts、MenuSettingsPanel.ts、MenuStatsPanel.ts、PausePanel.ts、ResultPanel.ts）全部是 ui-engineer lane 的显示组件 — gameplay-engineer 在并行 lane 期间改写 ui/ 层违反 lane 边界，且 6 文件一括置换超出「最小修正」。IMG-22～25/29（志向图标/事件卡框/标题 emblem）已由本 story 经 ASSET_KEYS 接线；仅 IMG-30 的 UI 组件置换未完成。**建议拆分为新 story（IMG-30 UI sheet 帧置换 — assignee: ui-engineer）**，已在 state/stories.yaml S-33 注记上报，Checkpoint 展示 |
| 2 | major | 资产加载失败被静默吞掉（无 FILE_LOAD_ERROR 监听、无日志） | **已处理**。game/src/scenes/BootScene.ts preload 冒头追加 `load.on(Phaser.Loader.Events.FILE_LOAD_ERROR)` → 失败时 `console.error('[AssetMissing] key=... url=... fallback 纹理で代替（Checkpoint 報告対象）')`。fallback 视觉保留为 lane 合流前的过渡（finding 1 拆分后由 UI 置换 story 自然消解） |
| 3 | major | 伙计立绘显示尺寸低于 assets.md 带（STAFF_AVATAR_HEIGHT=96、WAITER_MARKER_HEIGHT=112，实际 96–125px vs 规格 192–256px） | **暂不处理＋设计层决策上报**（reviewer 指示どおり）。根因是 MORNING.AVATARS 网格框 96px×5 列（MORNING.AVATAR_WIDTH/HEIGHT=96、S-05 布局）— 放大头像框/改布局/修订 acceptance 三案均属设计决策，超出本 story 的最小修正范围。WAITER_MARKER_HEIGHT 提升到 192 也会因柜台 y=648＋origin 中央导致下端越出 720px 画面（需 GAME_LAYOUT 调整）。已记录本注记并在 Checkpoint 展示，请 game-designer/人类裁定 |
| 4 | minor | sheet 键的 fallback 纹理是单帧 256x256 — IMG-30 接线后帧引用会指向不存在的帧 | **已处理**。BootScene.generateFallbackSheetTexture 新设: 按 IMG_SHEET_FRAME（WIDTH/HEIGHT×COLS/ROWS）生成 3x2 网格纹理并 `texture.add(index, ...)` 登记帧 0–5 — fallback 状态下 UI 帧引用也成立。IMG_SHEET_FRAME 追加 COLS/ROWS 字段、ASSET_FALLBACK 追加 CELL_ALPHAS（帧区画的市松明度差） |
| 5 | minor | visualAssets 的防御降级路径无告警（dishSpriteKey 越界钳位、staffSpriteKey 未登记 id 返回 null 后静默省略） | **已处理**。两函数追加 `console.warn('[visualAssets] ...')`＋头部注释记载 warn（非 error）的正当性 — 均为回復可能路径（钳位后仍返回正当键/省略立绘で継続），warn 用于暴露呼出側 bug |
| 6 | minor | SPRITE_PLATE_ALPHA 注记「立绘の後ろに敷く」与实描画顺（sprite 后 add = 蒙层）相反 | **已处理（蒙层側に統一）**。config 注记修正为「立绘の上に重ねる半透明ステージティント」。描画順は変更しない — S-07 成长差分（色调＋缩放）の色调はこの 28% 蒙層で表現しており、垫底に变更すると STAGE_TINTS が立绘に隱れて S-07 acceptance（色调差分）が消失するため。GameplayView 側コメント（plate の色は半透明 — 立绘の視認性を壊さない）とも一致 |
| 7 | minor | SPRITE_DISPLAY.EVENT_CARD_WIDTH 死常量（宽度由源图 1024x1536 = 2:3 的等比缩放隐式决定） | **已处理（删除）**。本 story（S-33 commit）で追加した自常量であり参照 0 件を確認済み（grep 全 src）。等比縮小が幅 400 を自動的に満たす旨を EVENT_CARD_HEIGHT の注记に記載 |
| 8 | minor | 柜台/出餐口が GAMEPLAY.SERVE_FILL の纯色矩形のまま（コード注記も「IMG 资产の無い施設プレート」と自認）— acceptance「画面中不存在纯色/渐变占位矩形」との緊張。IMG-01～30 には柜台/出餐口の単体アセットが存在しない（IMG-01～03 背景には描写あり） | **暂不处理＋設計層裁定上报**。理由: (1) design/assets.md IMG-01～30 に対話用柜台/出餐口の単体 sprite が存在せず、新規アセット生成は 30 枚上限の再設計＋art-director 職域、acceptance 改訂は game-designer/人間の裁定事項 — いずれも本 story の最小修正範囲外。(2) コード上は無秩序な占位残置ではなく枠線付きパネル＋ラベル（LABEL_COUNTER/LABEL_SERVE_WINDOW）で施設としての意図的表現になっており、機能的区別は担保済み。(3) 解消経路は 2 択 — 「施設パネル設計」として acceptance 注記で明確化するか、finding 1 の IMG-30 拆分 story（ui-engineer）で sheet 帧素材による覆盖に含める。stories.yaml S-33 notes に注記し Checkpoint 展示 |

（补记 2026-09-04T00:30:11Z `date -u` 実測: 本 iteration の再実行時に finding 8 が処理表から漏記していたため追記。コード修正は不要 — 記録补完のみ）

- 验证: `cd game && npm run typecheck` → 自分の編集ファイル（state/ 配下のみ。TS 変更なし）起因エラー 0。現時点の残存 1 件（src/ui/Hud.ts(10,15) TS2724）は ui-engineer lane の WIP — lane 規律どおり無視（tech-stack.md「検証コマンド」）。並行 lane 期間のため `npm run build` は不実施 — lane 合流後のバッチ検証が最終確認）
- 残存（Checkpoint 展示対象）: finding 1（IMG-30 → UI 置換 story への拆分推奨）、finding 3（立绘表示サイズ — MORNING.AVATARS レイアウト制約の設計裁定）、finding 8（柜台/出餐口 SERVE_FILL 純色矩形 — 施設パネルとしての acceptance 注記 or IMG-30 拆分 story での覆盖）
